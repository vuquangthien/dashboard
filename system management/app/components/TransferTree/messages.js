/*
 * TransferList Messages
 *
 * This contains all the text for the TransferList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TransferList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TransferList component!',
  },
});
