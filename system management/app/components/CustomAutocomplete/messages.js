/*
 * AsyncAutocomplete Messages
 *
 * This contains all the text for the AsyncAutocomplete component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AsyncAutocomplete';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AsyncAutocomplete component!',
  },
});
