const styles = theme => ({
  root: {
    flexGrow: 1,
  },
  control: {
    padding: `${theme.spacing()}px ${theme.spacing(2)}px`,
  },
  fabControl: {
    marginLeft: 10,
  },
  flexEndGrid: {
    justifyContent: 'flex-end',
    display: 'flex',
  },
});

export default styles;
